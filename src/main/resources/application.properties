spring.application.name=comparison-tool
server.port=8080

# Allow large file uploads (HAR files can be big)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Session config
server.servlet.session.timeout=4h

# Logging
logging.level.com.fpt=INFO